var isGreaterThan = function isGreaterThan(lowerBound) {
  return function (props, propName, componentName) {
    var value = props[propName];

    if (typeof value === 'number' && !Number.isNaN(value) && value > lowerBound) {
      return null;
    }

    return new Error("".concat(propName, " in ").concat(componentName, " must be a non-NaN number greater than ").concat(lowerBound, "."));
  };
};

export default isGreaterThan;
//# sourceMappingURL=greaterThan.js.map